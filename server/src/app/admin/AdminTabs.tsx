// AdminTabs is no longer used — the admin area has only one page (users).
// Kept as an empty export to avoid breaking any stale imports during the
// transition. Safe to delete once confirmed unused.
export function AdminTabs(): null {
  return null;
}
