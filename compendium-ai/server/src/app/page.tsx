// Placeholder landing page. Replaced by an admin dashboard later.

export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center p-8">
      <div className="text-center">
        <h1 className="text-2xl font-semibold">Compendium</h1>
        <p className="mt-2 text-sm text-neutral-500">
          Self-hosted real-time vault. Sync + search + AI running.
        </p>
      </div>
    </main>
  );
}
